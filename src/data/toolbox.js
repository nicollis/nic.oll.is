import * as $ from '../models/tech'

export default {
  high: [
    $.Swift,
    $.Apple,
    $.Ruby,
    $.Rails,
    $.Javascript,
    $.React
  ],

  mid: [
    $.Python,
    $.Kotlin,
    $.Android,
    $.Vue,
    $.ObjC,
    $.CPP,
  ],

  low: [
    $.Elixir,
    $.Java,
    $.C,
    $.PHP,
    $.NodeJs,
    $.CSharp
  ],

  deploy: [
    $.Jupyter,
    $.Capistrano,
    $.Heroku,
    $.Saltstack,
    $.GoogleCloud,
    $.AWS
  ]
}
