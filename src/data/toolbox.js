import * as $ from '../models/tech'

export default {
  high: [
    $.Swift,
    $.Apple,
    $.Ruby,
    $.Rails,
    $.Javascript,
    $.React
  ],

  mid: [
    $.Kotlin,
    $.Android,
    $.Vue,
    $.Java,
    $.ObjC,
    $.CPP,
  ],

  low: [
    $.Elixir,
    $.Python,
    $.C,
    $.PHP,
    $.NodeJs,
    $.CSharp
  ],

  deploy: [
    $.Capistrano,
    $.Heroku,
    $.Saltstack,
    $.GoogleCloud,
    $.Firebase,
    $.AWS
  ]
}