import * as $ from '../models/tech'

export default {
  high: [
    $.Swift,
    $.Apple,
    $.Ruby,
    $.Rails,
    $.Javascript,
    $.React
  ],

  mid: [
    $.Python,
    $.Kotlin,
    $.Android,
    $.Vue,
    $.ObjC,
    $.CPP,
  ],

  low: [
    $.R,
    $.Elixir,
    $.Java,
    $.C,
    $.NodeJs,
    $.CSharp
  ],

  deploy: [
    $.Jupyter,
    $.Capistrano,
    $.Heroku,
    $.Saltstack,
    $.GoogleCloud,
    $.AWS
  ]
}
